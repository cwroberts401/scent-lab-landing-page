// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://cwroberts401.github.io',
    base: '/scent-lab-landing-page',
});
